export interface IBrandWavesProps {
  /** Position and size the waves. Give a fixed height — the lines keep their
   *  proportions and are cropped at the sides to fit the width. */
  className?: string;
}
